project.name = zyarticle
project.default_charset = utf-8

[id]
type = id

[title]
type = title

[content]
type = body

[category_name]
type = string
index = self
tokenizer = full

[tags]
type = string
index = both
tokenizer = split(,)

[pub_time]
type = date
index = none

